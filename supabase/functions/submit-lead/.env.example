# supabase/functions/submit-lead/.env
# These are set via Supabase dashboard, NOT committed to git
# Dashboard → Edge Functions → submit-lead → Secrets

# From resend.com → API Keys (free tier: 3,000 emails/month)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# From Supabase → Settings → API → service_role key (keep secret — server-side only)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Auto-injected by Supabase — no action needed
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
